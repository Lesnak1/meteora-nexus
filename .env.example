# Environment variables example file
# No environment variables needed - this is a frontend-only application

# Optional: Solana network (defaults to mainnet-beta)
# NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta

# Optional: Custom RPC URL (uses public endpoint by default)
# NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com